version: '3'

services:
  web:
    image: python:3.9.19-alpine3.20
    volumes:
      - .:/athenaguard
    working_dir: /athenaguard
    command: sh -c "pip install -r requirements.txt && python manage.py runserver 0.0.0.0:8000"
    ports:
      - 8000:8000

